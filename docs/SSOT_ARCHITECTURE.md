# SSOT (Single Source of Truth) 架構文檔

## 📋 概述

本系統採用 **SSOT (Single Source of Truth)** 原則，所有遊戲狀態以伺服器端資料庫為唯一真實來源。

## 🎯 SSOT 核心原則

### 1. 集中式資料儲存

- **唯一真實來源**：SQLite 資料庫（透過 Drizzle ORM）
- **儲存位置**：`server/database/index.ts`
- **資料表**：
  - `rooms` - 房間基本資訊
  - `players` - 玩家資料
  - `drawSequences` - 抽獎序列
  - `drawOrders` - 抽獎順序
  - `drawResults` - 抽獎結果

### 2. 資料流向

```
┌─────────────────────────────────────────────────────────────┐
│                      SSOT 資料流                             │
└─────────────────────────────────────────────────────────────┘

客戶端 A          客戶端 B          客戶端 C
   ↓                 ↓                 ↓
   └─────────────────┴─────────────────┘
                     ↓
              ┌──────────────┐
              │  WebSocket   │  ← 通信層（即時事件）
              │   Server     │
              └──────────────┘
                     ↓
              ┌──────────────┐
              │ Room Service │  ← 業務邏輯層
              └──────────────┘
                     ↓
              ┌──────────────┐
              │   SQLite DB  │  ← 唯一真實來源 (SSOT)
              │   (Drizzle)  │
              └──────────────┘
```

### 3. 狀態同步機制

#### A. 事件驅動（主要）

```typescript
// 所有狀態變更都會：
1. 更新資料庫
2. 從資料庫載入最新狀態
3. 廣播給所有客戶端
```

#### B. 定期同步（備援）

```typescript
// 每 3 秒
syncState() → 伺服器回傳最新狀態 → 客戶端更新

// 每 5 秒（遊戲中）
validateState() → 伺服器驗證並校正 → 客戶端自動修正差異
```

#### C. Pre-flight 檢查（開始前）

```typescript
// 遊戲開始前
runPreflightCheck() → 測試所有玩家連線 → 確保通信正常
```

## 🔄 關鍵操作流程

### 創建房間

```typescript
1. 客戶端: createRoom(playerName, settings)
2. 伺服器: roomService.createRoom()
   ├─ 插入 rooms 表
   ├─ 插入 players 表
   ├─ 生成 reconnectToken
   └─ 回傳完整房間狀態（來自 DB）
3. 客戶端: 接收 room_created 事件，更新 roomState.value
```

### 加入房間

```typescript
1. 客戶端: joinRoom(roomId, playerName, isSpectator)
2. 伺服器: roomService.joinRoom()
   ├─ 從 DB 載入房間
   ├─ 驗證加入條件
   ├─ 插入新玩家到 players 表
   ├─ 更新 rooms 表
   ├─ 從 DB 重新載入完整狀態
   └─ 廣播 player_joined 給所有人
3. 所有客戶端: 接收事件，更新 roomState.value
```

### 開始遊戲

```typescript
1. Pre-flight 檢查
   ├─ 發送測試訊息給所有玩家
   ├─ 虛擬玩家自動回應
   ├─ 等待所有真實玩家回應（5秒超時）
   └─ 檢查失敗則中止

2. 伺服器: roomService.startGame(seed)
   ├─ 從 DB 載入房間
   ├─ 生成抽獎序列（generateDrawSequence）
   ├─ 插入 drawSequences 表
   ├─ 插入 drawOrders 表
   ├─ 更新 rooms.gameState = 'playing'
   ├─ 從 DB 重新載入完整狀態
   └─ 立即廣播 game_started

3. 所有客戶端:
   ├─ 接收事件，更新 roomState.value
   ├─ 啟動定期驗證（每5秒）
   └─ 重置動畫狀態
```

### 執行抽獎

```typescript
1. 客戶端: performDraw() 或 hostPerformDraw(drawerId)
2. 伺服器: roomService.performDraw()
   ├─ 從 DB 載入房間
   ├─ 驗證抽獎條件
   ├─ 插入 drawResults 表
   ├─ 更新 rooms.currentIndex
   ├─ 從 DB 重新載入完整狀態
   └─ 立即廣播 draw_performed
3. 所有客戶端:
   ├─ 接收事件，觸發動畫
   └─ 動畫結束後更新顯示
```

### 下一位抽獎者

```typescript
1. 客戶端: nextDrawer()
2. 伺服器: roomService.nextDrawer()
   ├─ 從 DB 載入房間
   ├─ 驗證當前抽獎者是否完成
   ├─ currentIndex++
   ├─ 檢查是否完成（currentIndex >= drawOrder.length）
   ├─ 若完成：更新 gameState = 'complete'
   ├─ 更新 rooms 表
   ├─ 從 DB 重新載入完整狀態
   └─ 立即廣播 next_drawer 或 game_complete
3. 所有客戶端: 接收事件，更新狀態
```

### 重連機制

```typescript
1. 客戶端: 檢測到斷線，自動嘗試重連
2. 伺服器: roomService.handleReconnect(roomId, token, odId)
   ├─ 驗證 reconnectToken
   ├─ 檢查 token 是否過期（2小時）
   ├─ 從 DB 載入房間
   ├─ 更新玩家 isConnected = true
   ├─ 保留原始 role, isCreator, isHost
   ├─ 更新 players 表
   ├─ 從 DB 重新載入完整狀態
   └─ 回傳 reconnect_success
3. 客戶端: 接收完整房間狀態，恢復遊戲
```

## 🛡️ 資料一致性保證

### 1. 寫入後重新載入

```typescript
// ✅ 正確做法（SSOT）
async function updateRoom(roomId: string) {
  // 1. 寫入資料庫
  await db
    .update(schema.rooms)
    .set({ gameState: "playing" })
    .where(eq(schema.rooms.id, roomId));

  // 2. 從資料庫重新載入（SSOT）
  const room = await loadRoomFromDb(roomId);

  // 3. 廣播最新狀態
  broadcastToRoom(roomId, { type: "room_updated", payload: { room } });
}

// ❌ 錯誤做法（本地修改）
function updateRoomWrong(room: Room) {
  room.gameState = "playing"; // 僅修改記憶體
  // 沒有寫入 DB，沒有重新載入，狀態不一致！
}
```

### 2. 客戶端只讀原則

```typescript
// ✅ 客戶端永遠不直接修改 roomState
roomState.value = serverProvidedState; // 僅接收伺服器狀態

// ❌ 客戶端不應該這樣做
roomState.value.gameState = "playing"; // 違反 SSOT！
```

### 3. 立即廣播關鍵事件

```typescript
// 關鍵操作使用立即廣播（跳過節流）
broadcastToRoom(roomId, message, excludeOdId, true); // immediate: true
```

關鍵事件：

- `game_started` - 遊戲開始
- `draw_performed` - 抽獎完成
- `next_drawer` - 下一位
- `game_complete` - 遊戲結束

### 4. 定期驗證與校正

```typescript
// 每 5 秒（僅在遊戲中）
validateState() {
  // 1. 客戶端發送本地狀態
  // 2. 伺服器比對 DB 狀態
  // 3. 發現差異則自動校正
  // 4. 回傳正確狀態
}
```

## 📊 狀態轉換追蹤

### 房間狀態機

```
waiting → playing → complete
   ↓         ↓
   └────────┴────→ disbanded (解散)
```

### 玩家狀態

```
isConnected: boolean    // 連線狀態
isReady: boolean       // 準備狀態
role: player|spectator // 角色
isHost: boolean        // 主機
isCreator: boolean     // 創建者
isVirtual: boolean     // 虛擬玩家（AI BOT）
```

### 關鍵日誌

所有狀態轉換都會記錄：

```typescript
console.log("[RoomService] State transition:", {
  roomId,
  from: oldState,
  to: newState,
  trigger: "start_game",
  timestamp: Date.now(),
});
```

## 🔍 驗證機制

### Pre-flight 檢查

在遊戲開始前執行：

```typescript
1. 生成唯一 testId
2. 發送測試訊息給所有玩家
3. 虛擬玩家立即自動回應
4. 等待真實玩家回應（5秒超時）
5. 檢查所有玩家是否回應
6. 失敗則中止遊戲開始
```

### 狀態驗證

遊戲進行中每 5 秒執行：

```typescript
1. 客戶端發送本地關鍵狀態
2. 伺服器從 DB 載入真實狀態
3. 比對以下欄位：
   - gameState
   - currentIndex
   - results.length
   - players.length
4. 發現差異則：
   - 記錄警告日誌
   - 回傳正確狀態
   - 客戶端自動更新
   - 顯示提示訊息
```

## 🚨 錯誤處理

### 資料庫錯誤

```typescript
try {
  await saveRoomToDb(room);
} catch (error) {
  console.error("[DB Error]", error);
  // 不更新客戶端狀態，保持一致性
}
```

### 網路錯誤

```typescript
// WebSocket 斷線
1. 客戶端自動重連
2. 使用 reconnectToken 恢復身份
3. 從伺服器獲取最新狀態
4. 無縫恢復遊戲
```

### 狀態不一致

```typescript
// 自動校正機制
1. 定期驗證檢測到差異
2. 伺服器回傳正確狀態
3. 客戶端自動更新
4. 顯示「狀態已修正」提示
```

## 📈 效能優化

### 1. 資料庫索引

```sql
-- 房間查詢
CREATE INDEX idx_rooms_id ON rooms(id);

-- 玩家查詢
CREATE INDEX idx_players_room ON players(roomId);
CREATE INDEX idx_players_id ON players(playerId);

-- Token 驗證
CREATE INDEX idx_tokens_room_token ON reconnect_tokens(roomId, token);
```

### 2. 廣播節流

```typescript
// 非關鍵事件使用 50ms 節流
throttledBroadcast(roomId, message);

// 關鍵事件立即廣播
broadcastToRoom(roomId, message, excludeOdId, true);
```

### 3. 連線池管理

```typescript
// WebSocket 連線映射
const peers = new Map<string, PeerData>();

// 快速查找房間內所有玩家
function getRoomPeers(roomId: string) {
  return Array.from(peers.values()).filter((p) => p.roomId === roomId);
}
```

## 🎓 最佳實踐

### DO ✅

1. **所有狀態變更都經過資料庫**
2. **修改後立即重新載入**
3. **客戶端僅接收，不修改**
4. **關鍵操作使用立即廣播**
5. **定期驗證狀態一致性**
6. **記錄所有狀態轉換日誌**

### DON'T ❌

1. **不要僅修改記憶體狀態**
2. **不要跳過資料庫寫入**
3. **不要信任客戶端本地計算**
4. **不要在客戶端直接修改 roomState**
5. **不要忽略驗證失敗**

## 🔧 維護指南

### 添加新功能

```typescript
// 1. 定義資料庫 schema（如需要）
export const newFeature = sqliteTable('new_feature', {
  id: integer('id').primaryKey(),
  roomId: text('roomId').notNull(),
  // ...
});

// 2. 在 roomService 添加業務邏輯
export async function updateFeature(roomId: string, data: any) {
  // 寫入 DB
  await db.insert(schema.newFeature).values(data);

  // 重新載入
  const room = await loadRoomFromDb(roomId);

  // 廣播
  broadcastToRoom(roomId, { type: 'feature_updated', payload: { room } });

  return room;
}

// 3. 在 ws.ts 添加訊息處理
case 'update_feature': {
  const room = await roomService.updateFeature(roomId, msg.payload);
  // ...
}

// 4. 在客戶端添加事件監聽
on('featureUpdated', (room) => {
  roomState.value = room; // SSOT：接收伺服器狀態
});
```

### 除錯步驟

1. 檢查伺服器日誌（狀態轉換）
2. 驗證資料庫內容（`rooms`, `players`, etc.）
3. 檢查 WebSocket 訊息（Chrome DevTools）
4. 手動觸發狀態驗證（`validateState()`）
5. 比對客戶端與伺服器狀態

---

**總結**：本系統嚴格遵循 SSOT 原則，所有資料以伺服器資料庫為準，搭配多重驗證機制，確保資料一致性、準確性和完整性。
